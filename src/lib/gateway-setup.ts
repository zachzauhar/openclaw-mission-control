/**
 * Stub gateway setup — our agent doesn't need gateway initialization.
 * These are no-ops kept for compatibility with components that import them.
 */

export function triggerResponsesEndpointSetup(): void {}
export async function waitForResponsesEndpoint(): Promise<void> {}
